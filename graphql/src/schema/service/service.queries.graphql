extend type Query {
  # Retorna a lista completa de serviços para a seleção no frontend
  services: [Service!]!
  
  # Procura um serviço específico pelo ID (útil para detalhes ou faturamento)
  service(id: ID!): Service
}