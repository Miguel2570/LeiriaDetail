extend type Query {
    registosServices(status: ServiceStatus, clientId: Int, date: String): RegistosData!
    registosActiveServices: RegistosData!
    registosService(id: Int!): RegistosData!
    registosStats: RegistosData!
    registosClientHistory(clientId: Int!): RegistosData!
}