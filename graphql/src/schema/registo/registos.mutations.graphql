extend type Mutation {
    createService(input: CreateServiceInput!): RegistosData!
    startService(serviceId: Int!): RegistosData!
    updateServiceChecklist(input: UpdateChecklistInput!): RegistosData!
    completeService(input: CompleteServiceInput!): RegistosData!
}